#include<stdio.h>
int main(){

//先定义一个数组
int a[10];

//这里先定以一个数组代表的含义是 先定义了一个长度为10的数组
//然后用下表来为数组赋值
int i=0;
for(i=0;i<10;++i)
{
	//这里不对数组a[i]进行赋值
	//直接打印输出数组a[i];
	//printf("%d",a[i]);
	//这样输出的就是随机赋值的数组
	a[i]=i+1;
}
	//如何自定义数组的值
	//先赋值
	//a[i]=i+1
	//后顺序打印输出
	//a[i];;
for(i=0;i<10;++i)
{
printf("%d;",a[i]);
}

printf("\n");
return 0;




}
